{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-local-backend",
  "description": "Allow the trusted SubGen desktop webview to select a local video without uploading a duplicate.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:8765/*"]
  },
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
